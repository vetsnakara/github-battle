import { createUseStyles } from 'react-jss'

export default createUseStyles({
  container: {
    textAlign: 'center'
  },

  battleBtn: {
    backgroundColor: '#000',
    padding: '10px 60px',
    color: '#fff',
    textDecoration: 'none',
    textTransform: 'uppercase',
    borderRadius: 3,
    letterSpacing: '.3em'
  }
})
